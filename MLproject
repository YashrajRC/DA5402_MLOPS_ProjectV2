name: mental_health_classifier

python_env: python_env.yaml

entry_points:
  train:
    parameters:
      model: {type: string, default: "logreg"}
    command: "python -m src.training.train --model {model}"

  main:
    parameters:
      model: {type: string, default: "logreg"}
    command: "python -m src.training.train --model {model}"
